import { Context, PublicKey } from '@metaplex-foundation/umi';
export declare type CompressionPrograms = {
    logWrapper: PublicKey;
    compressionProgram: PublicKey;
};
export declare function getCompressionProgramsForV1Ixs(context: Pick<Context, 'programs' | 'eddsa' | 'rpc'>): Promise<CompressionPrograms>;
